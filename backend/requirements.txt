fastapi==0.141.1
uvicorn[standard]==0.52.0

fastapi==0.141.1
uvicorn[standard]==0.52.0

# DB
sqlalchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12  # для Alembic-міграцій (sync)
asyncpg==0.31.0          # для самого застосунку (async)

# env / config
python-decouple==3.8
dj-database-url==3.1.2

# auth
PyJWT==2.13.0
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.4
passlib==1.7.4
# passlib 1.7.4 (остання версія, з 2020р.) несумісний з bcrypt>=4.1 -
# видає нешкідливий, але шумний варнінг "error reading bcrypt version".
# Тому свідомо фіксуємо 4.0.1, а не найновішу.
bcrypt==4.0.1

# storage (Cloudflare R2, S3-compatible)
boto3==1.43.58

# uploads
python-multipart==0.0.32
python-slugify==8.0.4
Pillow==12.3.0